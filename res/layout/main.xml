<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

    <Button
        android:id="@+id/btnConnect"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Connect" android:onClick="connect" android:filterTouchesWhenObscured="false"/>

    <Button
        android:id="@+id/btnDisconnect"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Disconnect" android:layout_below="@+id/btnConnect" android:onClick="disconnect"/>

    <Button
        android:id="@+id/btnForward"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Forward" android:layout_below="@+id/btnDisconnect" android:longClickable="true"/>

    <SeekBar
        android:id="@+id/skbTurnRate"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" android:layout_below="@+id/btnForward" android:layout_marginLeft="20dip" android:layout_marginRight="20dip"/>

</RelativeLayout>